from setuptools import setup, find_packages

setup(
    name="social-structure-learning",
    version="0.1",
    description="",
    author="Max Taylor-Davies",
    url="",
    packages=find_packages(exclude=["contrib", "docs", "tests"]),
)
